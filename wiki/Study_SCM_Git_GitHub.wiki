#summary GitHub Study.
#sidebar TableOfContents


{{{
$ git clone <repo> -b gh-pages
}}}


{{{
$ cd repository

$ git checkout --orphan gh-pages
# Creates our branch, without any parents (it's an orphan!)
# Switched to a new branch 'gh-pages'

$ git rm -rf .
# Remove all files from the old working tree
# rm '.gitignore'
}}}


{{{
$ echo "My GitHub Page" > index.html
$ git add index.html
$ git commit -a -m "First pages commit"
$ git push origin gh-pages
}}}


{{{
$ git branch --set-upstream-to=origin/gh-pages
}}}

==[https://help.github.com/articles/setting-up-a-custom-domain-with-github-pages Setting up a custom domain with GitHub Pages]==

===参考资料===
`[1].` https://help.github.com/articles/creating-project-pages-manually<br>
`[2].` https://help.github.com/articles/setting-up-a-custom-domain-with-pages<br>